
{{#if productsExist}}

<table class="table table-dark w-75 mx-auto">
  <thead>
    <tr>
      <th scope="col">Producto</th>
      <th scope="col">Precio</th>
      <th scope="col">Cantidad</th>
      <th scope="col">Total por producto</th>
      <th scope="col">Total del carrito</th>
    </tr>
  </thead>

<tbody>
  {{#each products}}
    <tr>
      <td>{{title}}</td>
      <td>${{price}}</td>
      <td>{{quantity}}</td>
      <td>${{totalProduct}}</td>
      <td><button class="btn btn-danger">X</button></td>
      <td></td>
    </tr>
  
  {{/each}}

  <tr>
    <td></td>
    <td></td>
    <td></td>
    <td></td>
    <td>${{cartTotal}}</td>
  </tr>

    </tbody>
</table>

{{else}}
  <p class="text-center fw-bold text-white">El carrito está vacio</p>

{{/if}}





